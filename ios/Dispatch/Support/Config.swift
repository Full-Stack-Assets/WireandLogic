import Foundation

/// Site-level configuration shared across the app. `baseURL` is used to build
/// canonical article links for sharing — point it at your deployment.
enum SiteConfig {
    static let baseURL = "https://dispatch.dev"
}
